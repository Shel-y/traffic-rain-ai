# Dependencias mínimas para entrenamiento del modelo ML.
# NO incluye braket, sagemaker, osmnx ni streamlit.
# Usar en un entorno virtual limpio si hay conflictos.

pandas>=1.5
numpy>=1.23
scikit-learn>=1.2
joblib>=1.2
scipy>=1.9
